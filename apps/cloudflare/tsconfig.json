{
  "extends": [
    "../../tsconfig.base.json",
    "../../tsconfig.workspace-paths.json"
  ],
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "vitest/globals"],
    "noEmit": true
  },
  "include": ["src/**/*.ts"]
}
